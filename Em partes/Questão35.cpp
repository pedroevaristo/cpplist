#include<iostream>
#include<bits/stdc++.h>
#include<string.h>
#include<math.h>

using namespace std;

int main(){
int v[5];
int n, x, a;

    for(x = 0; x < 5; x++){
        cin>> v[x];
    }

    for(x = 0; x < 5; x++){
     n = pow(v[x] , 2);
    cout<< n <<endl;
    }


}